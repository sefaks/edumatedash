export interface Program {
    day: string;  // Dersin günü (örn. "Pazartesi")
    startTime: string;  // Dersin başlangıç saati (örn. "09:00")
    endTime: string;  // Dersin bitiş saati (örn. "10:00")
    course: string;  // Dersin adı (örn. "Matematik")
  }